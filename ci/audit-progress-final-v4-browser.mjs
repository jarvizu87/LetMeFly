#!/usr/bin/env node
import fs from 'node:fs'
import path from 'node:path'
import { createRequire } from 'node:module'
import { fileURLToPath } from 'node:url'

const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..')
const target=path.join(root,'.build-src','letmefly_app')
const outDir=path.join(target,'PROGRESS_FINAL_V4_AUDIT')
fs.mkdirSync(outDir,{recursive:true})
const requireFromTarget=createRequire(path.join(target,'package.json'))
const {chromium}=requireFromTarget('playwright-core')
const chromeBin=process.env.CHROME_BIN
if(!chromeBin)throw new Error('CHROME_BIN is required')
const report={result:'PASS',passes:[],failures:[],observations:{}}
const pass=(label,detail='')=>{report.passes.push({label,detail});console.log(`PASS  ${label}${detail?` — ${detail}`:''}`)}
const fail=(label,detail='')=>{report.result='FAIL';report.failures.push({label,detail});console.log(`FAIL  ${label}${detail?` — ${detail}`:''}`)}
async function firstVisible(locator){const count=await locator.count();for(let i=0;i<count;i+=1){const item=locator.nth(i);if(await item.isVisible().catch(()=>false))return item}return null}
async function dismissInstall(page){const button=await firstVisible(page.locator('button,a,[role="button"]').filter({hasText:/^\s*Not now\s*$/i}));if(button)await button.click({timeout:1500}).catch(()=>null)}
async function bootstrap(page){await page.goto('http://127.0.0.1:4173/',{waitUntil:'domcontentloaded',timeout:20000});await page.waitForSelector('body',{timeout:10000});for(let i=0;i<12;i+=1){await dismissInstall(page);const create=await firstVisible(page.locator('button,a,[role="button"]').filter({hasText:/CREATE LOCAL ATHLETE/i}));if(create){const modal=create.locator('xpath=ancestor::*[contains(@class,"modal-backdrop")][1]');const input=await firstVisible(modal.locator('input[type="text"],input:not([type])'))||await firstVisible(page.locator('input[type="text"],input:not([type])'));if(!input)throw new Error('Athlete display-name input missing');await input.fill('Progress Final QA Athlete');await create.click({timeout:4000});await page.waitForTimeout(500);break}await page.waitForTimeout(160)}for(let i=0;i<5;i+=1){await dismissInstall(page);await page.waitForTimeout(100)}}
async function seed(page){return page.evaluate(async()=>{const reqp=req=>new Promise((resolve,reject)=>{req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)}),db=await new Promise((resolve,reject)=>{const req=indexedDB.open('letmefly-private');req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)}),athletes=await reqp(db.transaction('athletes','readonly').objectStore('athletes').getAll()),athlete=athletes[0];if(!athlete)throw new Error('No local athlete');const put=async(storeName,value)=>{const tx=db.transaction(storeName,'readwrite'),req=tx.objectStore(storeName).put(value);await reqp(req);await new Promise((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error)})};const today=new Date(),older=new Date(today);older.setDate(older.getDate()-2);const t1=older.toISOString(),t2=today.toISOString(),s1='qa-progress-final-session-1',s2='qa-progress-final-session-2',e1='qa-progress-final-bench-1',e2='qa-progress-final-bench-2';await put('workoutSessions',{id:s1,athlete_id:athlete.id,status:'completed',workout_name:'Bench QA 1',started_at:t1,completed_at:t1,created_at:t1,updated_at:t1});await put('workoutSessions',{id:s2,athlete_id:athlete.id,status:'completed',workout_name:'Bench QA 2',started_at:t2,completed_at:t2,created_at:t2,updated_at:t2});await put('workoutExercises',{id:e1,athlete_id:athlete.id,workout_session_id:s1,exercise_key:'bench-press',exercise_name_snapshot:'Bench Press',prescription_snapshot:{category:'strength'},created_at:t1,updated_at:t1});await put('workoutExercises',{id:e2,athlete_id:athlete.id,workout_session_id:s2,exercise_key:'bench-press',exercise_name_snapshot:'Bench Press',prescription_snapshot:{category:'strength'},created_at:t2,updated_at:t2});await put('workoutSets',{id:'qa-progress-final-set-1',athlete_id:athlete.id,workout_session_id:s1,workout_exercise_id:e1,set_number:1,completed:true,load_value:100,load_unit:'lb',reps:5,rpe:7,completed_at:t1,created_at:t1,updated_at:t1});await put('workoutSets',{id:'qa-progress-final-set-2',athlete_id:athlete.id,workout_session_id:s2,workout_exercise_id:e2,set_number:1,completed:true,load_value:110,load_unit:'lb',reps:5,rpe:8,completed_at:t2,created_at:t2,updated_at:t2});await put('workoutSets',{id:'qa-progress-final-set-3',athlete_id:athlete.id,workout_session_id:s2,workout_exercise_id:e2,set_number:2,completed:true,load_value:100,load_unit:'lb',reps:6,rpe:8,completed_at:t2,created_at:t2,updated_at:t2});if(db.objectStoreNames.contains('bodyweightEntries')){await put('bodyweightEntries',{id:'qa-progress-final-body-1',athlete_id:athlete.id,value:230,unit:'lb',measured_at:t1,created_at:t1,updated_at:t1});await put('bodyweightEntries',{id:'qa-progress-final-body-2',athlete_id:athlete.id,value:228,unit:'lb',measured_at:t2,created_at:t2,updated_at:t2})}db.close();return{athleteId:athlete.id}})}
async function clickTab(page,name){const tab=page.locator(`[data-pg-tab="${name}"]`).first();await tab.waitFor({state:'visible',timeout:8000});await tab.focus();await tab.click();await page.waitForSelector(`[data-pg-panel="${name}"]`,{timeout:8000});await page.waitForTimeout(500)}

const browser=await chromium.launch({headless:true,executablePath:chromeBin,args:['--no-sandbox','--disable-dev-shm-usage']})
const context=await browser.newContext({viewport:{width:412,height:915},isMobile:true,hasTouch:true})
const page=await context.newPage()
try{
  await bootstrap(page);report.observations.seed=await seed(page);await page.evaluate(()=>{localStorage.setItem('letmefly_progress_dashboard_range_v1','30d');location.hash='#/progress'});await page.waitForSelector('#lmf-progress-dashboard-v1',{timeout:12000});await page.waitForFunction(()=>window.__LMF_PROGRESS_FINALIZE__?.version===4,null,{timeout:10000}).catch(()=>null)

  await clickTab(page,'strength');await page.waitForSelector('.lmf-pg-strength-v4',{timeout:8000});let text=await page.locator('[data-pg-panel="strength"]').innerText();if(/WORKING-SET POINTS/i.test(text)&&/TREND-READY LIFTS/i.test(text)&&/ACTUAL 1RM.*tested\/manual strength record/is.test(text)&&/TM.*program loading input/is.test(text)&&/e1RM.*calculated from completed sets/is.test(text))pass('Strength records remain separated and explained');else fail('Strength records remain separated and explained',text.slice(0,1600));if(/Bench Press/i.test(text)&&/Early trend|Established history/i.test(text))pass('Strength trend evidence uses completed-set history');else fail('Strength trend evidence uses completed-set history',text.slice(0,1600))

  await clickTab(page,'body');await page.waitForSelector('.lmf-pg-body-v4',{timeout:8000});text=await page.locator('[data-pg-panel="body"]').innerText();if(/7-DAY AVERAGE/i.test(text)&&/229 lb/i.test(text)&&/RANGE ENTRIES/i.test(text))pass('Body trend emphasizes rolling context','7-day average 229 lb');else fail('Body trend emphasizes rolling context',text.slice(0,1400));const goalInput=page.locator('[data-body-goal-value]').first();await goalInput.fill('200');await page.locator('[data-body-goal-form] button[type="submit"]').click();await page.waitForTimeout(900);const savedGoal=await page.evaluate(async athleteId=>{const db=await new Promise((resolve,reject)=>{const req=indexedDB.open('letmefly-private');req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)}),goal=await new Promise(resolve=>{const req=db.transaction('athletes','readonly').objectStore('athletes').get(athleteId);req.onsuccess=()=>resolve(req.result?.progress_bodyweight_goal_v1||null);req.onerror=()=>resolve(null)});db.close();return goal},report.observations.seed.athleteId);report.observations.savedGoal=savedGoal;if(savedGoal?.value===200&&savedGoal?.unit==='lb'&&savedGoal?.start_value===228)pass('Bodyweight goal stored privately','200 lb from 228 lb start');else fail('Bodyweight goal stored privately',JSON.stringify(savedGoal))

  await clickTab(page,'prs');await page.waitForSelector('.lmf-pg-pr-v4',{timeout:8000});text=await page.locator('[data-pg-panel="prs"]').innerText();if(/WEIGHT PR/i.test(text)&&/110 lb/i.test(text))pass('Auto weight PR detection','110 lb');else fail('Auto weight PR detection',text.slice(0,1800));if(/REP PR/i.test(text)&&/6 reps/i.test(text)&&/at 100 lb/i.test(text))pass('Auto rep PR detection','6 reps at 100 lb');else fail('Auto rep PR detection',text.slice(0,1800));if(/VOLUME PR/i.test(text)&&/1,150 lb-reps/i.test(text))pass('Auto session-volume PR detection','1,150 lb-reps');else fail('Auto session-volume PR detection',text.slice(0,1800));if(/not written back as official PR records/i.test(text)&&/First records establish baselines/i.test(text)===false)pass('Calculated PRs stay separate from official records');else if(/not written back as official PR records/i.test(text))pass('Calculated PRs stay separate from official records');else fail('Calculated PRs stay separate from official records',text.slice(0,1800))

  await clickTab(page,'overview');await page.waitForSelector('.lmf-pg-overview-v4',{timeout:8000});text=await page.locator('[data-pg-panel="overview"]').innerText();if(/PROGRESS SIGNALS/i.test(text)&&/STRENGTH/i.test(text)&&/CONDITIONING/i.test(text)&&/BODY/i.test(text)&&/AUTO PRs/i.test(text))pass('Overview five-second signal layer');else fail('Overview five-second signal layer',text.slice(0,1600));if(/do not rewrite Crownforge, Black Crown, or today’s workout/i.test(text))pass('Overview preserves program authority');else fail('Overview preserves program authority',text.slice(0,1000));const api=await page.evaluate(()=>window.__LMF_PROGRESS_FINALIZE__||null);if(api?.version===4)pass('Progress final analytics runtime','version 4');else fail('Progress final analytics runtime',JSON.stringify(api))
} catch(error){fail('Progress final v4 browser audit execution',error instanceof Error?error.message:String(error))}
finally{await page.screenshot({path:path.join(outDir,'final.png'),fullPage:true}).catch(()=>null);await browser.close()}
fs.writeFileSync(path.join(outDir,'report.json'),`${JSON.stringify(report,null,2)}\n`)
if(report.failures.length){console.error(`LetMeFly Progress final v4 browser audit: FAIL (${report.failures.length})`);process.exit(1)}
console.log(`LetMeFly Progress final v4 browser audit: PASS (${report.passes.length})`)
