--- src/main.ts	2026-09-06 03:29:57.000000000 +0000
+++ src/main.ts	2026-09-06 03:57:41.978862084 +0000
@@ -278,7 +278,7 @@
   const load = set.load_value ?? ''
   const rpe = set.rpe ?? ''
   const plate = typeof load === 'number' && set.load_unit === 'lb' ? formatPlates(calculatePlates(load)) : ''
-  return `<div class="set-row" data-set-id="${esc(set.id)}"><div class="set-label"><span>SET</span><strong>${esc(String(set.set_number))}</strong></div><div class="set-target-cell"><span>Target</span><strong>${esc(target)}</strong></div><div class="set-field"><label>Reps</label><input class="set-input reps-input" inputmode="numeric" value="${esc(String(reps))}"></div><div class="set-field"><label>Load</label><input class="set-input load-input" inputmode="decimal" value="${esc(String(load))}"><small>${esc(plate)}</small></div><div class="set-field rpe-field"><label>RPE</label><input class="set-input rpe-input" inputmode="decimal" value="${esc(String(rpe))}"></div><button class="set-check ${set.completed ? 'done' : ''}" data-action="toggle-set">${set.completed ? '✓' : '○'}</button></div>`
+  return `<div class="set-row" data-set-id="${esc(set.id)}"><div class="set-label"><span>SET</span><strong>${esc(String(set.set_number))}</strong></div><div class="set-target-cell"><span>Target</span><strong>${esc(target)}</strong></div><div class="set-field reps-field"><label>Reps</label><input class="set-input reps-input" inputmode="numeric" value="${esc(String(reps))}"></div><div class="set-field load-field"><label>Load</label><input class="set-input load-input" inputmode="decimal" value="${esc(String(load))}"><small>${esc(plate)}</small></div><div class="set-field rpe-field"><label>RPE</label><input class="set-input rpe-input" inputmode="decimal" value="${esc(String(rpe))}"></div><button class="set-check ${set.completed ? 'done' : ''}" data-action="toggle-set">${set.completed ? '✓' : '○'}</button></div>`
 }function programPage(): string {
   const embeddedWeeks = CROWNFORGE.weekData
   const weekCards = CROWNFORGE.weekData.map((week) => {
--- src/styles.css	2026-09-06 03:29:58.000000000 +0000
+++ src/styles.css	2026-09-06 03:57:41.979193718 +0000
@@ -107,3 +107,28 @@
 .status-dot.green{background:#4ade80;box-shadow:0 0 0 4px rgba(74,222,128,.10)}
 .status-dot.yellow{background:#facc15;box-shadow:0 0 0 4px rgba(250,204,21,.10)}
 .status-dot.red{background:#fb7185;box-shadow:0 0 0 4px rgba(251,113,133,.10)}
+
+/* Small-phone workout logging: preserve touch targets and readable prescription context. */
+@media (max-width:420px){
+  .set-row{
+    grid-template-columns:repeat(6,minmax(0,1fr));
+    grid-template-areas:
+      "label target target target target check"
+      "reps reps load load rpe rpe";
+    gap:8px 7px;
+    padding:10px 8px;
+    align-items:end;
+  }
+  .set-label{grid-area:label;align-self:center}
+  .set-target-cell{grid-area:target;min-width:0;align-self:center}
+  .reps-field{grid-area:reps}
+  .load-field{grid-area:load}
+  .rpe-field{grid-area:rpe}
+  .set-check{grid-area:check;width:44px;height:44px;justify-self:end;align-self:center}
+  .set-input{min-height:42px;padding:8px 7px;font-size:12px}
+  .set-target-cell strong{font-size:10px;line-height:1.35}
+  .set-field small{white-space:normal;line-height:1.25}
+}
+@media (max-width:360px){
+  .tm-grid{grid-template-columns:1fr}
+}
--- scripts/audit-command-ui.mjs	2026-09-06 03:33:44.000000000 +0000
+++ scripts/audit-command-ui.mjs	2026-09-06 04:00:08.244215160 +0000
@@ -2,6 +2,7 @@
 
 const main = fs.readFileSync(new URL('../src/main.ts', import.meta.url), 'utf8')
 const css = fs.readFileSync(new URL('../src/styles.css', import.meta.url), 'utf8')
+const serviceWorker = fs.readFileSync(new URL('../public/service-worker.js', import.meta.url), 'utf8')
 const checks = [
   ['all major routes', /type Route = 'home' \| 'train' \| 'program' \| 'progress' \| 'exercises' \| 'coach' \| 'profile' \| 'more'/],
   ['mobile primary navigation', /\['home'.*'primary'\].*\['train'.*'primary'\].*\['program'.*'primary'\].*\['progress'.*'primary'\]/s],
@@ -27,9 +28,13 @@
   ['responsive mobile breakpoint', /@media\s*\(max-width:\s*(?:760|780)px\)/],
   ['Command UI session track styling', /\.session-track button/],
   ['Command UI filter styling', /\.filter-chips button/],
+  ['small-phone set field semantics', /class="set-field reps-field".*class="set-field load-field"/s],
+  ['small-phone two-row set layout', /@media\s*\(max-width:420px\)[\s\S]*grid-template-areas:[\s\S]*reps reps load load rpe rpe/],
+  ['small-phone completion touch target', /\.set-check\{grid-area:check;width:44px;height:44px/],
+  ['Command UI PWA cache version', /letmefly-shell-v5-4-command-1/],
 ]
 
-const failures = checks.filter(([, pattern]) => !(pattern.test(main) || pattern.test(css))).map(([label]) => label)
+const failures = checks.filter(([, pattern]) => !(pattern.test(main) || pattern.test(css) || pattern.test(serviceWorker))).map(([label]) => label)
 if (failures.length) {
   console.error(JSON.stringify({ result: 'FAIL', failures }, null, 2))
   process.exit(1)
--- public/service-worker.js	2026-09-06 00:32:44.000000000 +0000
+++ public/service-worker.js	2026-09-06 04:00:08.243885867 +0000
@@ -1,4 +1,4 @@
-const CACHE_NAME = 'letmefly-shell-v5-rebuild-1'
+const CACHE_NAME = 'letmefly-shell-v5-4-command-1'
 const PRECACHE = ['/', '/manifest.webmanifest', '/icon-192.png', '/icon-512.png']
 
 self.addEventListener('install', (event) => {
