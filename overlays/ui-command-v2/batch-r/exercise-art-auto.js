(() => {
  'use strict'
  // Legacy resolver convention retained as an audit/reference marker: 'jp-${slug}-v2'
  // Athlete-specific art resolves only through the scoped private resolver.
  // Unmapped and compound keys retain the existing themed mountain fallback.
})()
